# Morphix
Morphix – A lightweight developer toolkit for transforming, analyzing, and converting data formats such as JSON, TOON, DTO structures, and TypeScript schemas. Built with React + Vite + TypeScript.
